import { EducationsSorterPipe } from './educations-sorter.pipe';

describe('EducationsSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new EducationsSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
