import { ExperiencesSorterPipe } from './experiences-sorter.pipe';

describe('ExperiencesSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperiencesSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
