import { EmploymentTypeFormatterPipe } from './employment-type-formatter.pipe';

describe('EmploymentTypeFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmploymentTypeFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
